import { MiliffynumberPipe } from './miliffynumber.pipe';

describe('MiliffynumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MiliffynumberPipe();
    expect(pipe).toBeTruthy();
  });
});
